const myArr = [12, 34, 45, 56]
const arr = myArr.entries() //This returns array iterator for each element of original array in key value pair
console.log(arr.next().value)
console.log(arr.next().value)